This folder contains external dependencies that needed to be added into the solution

It also contains installation instructions for anything that is not automatically pulled into the build via the MODULE file.